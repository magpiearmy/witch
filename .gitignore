# Godot 4+ specific ignores
.godot/
/android/

# Editor / tool scratch files
*.tmp
*~
*.kra~

# Per-developer editor tooling (platform-specific binaries, not project code).
# addons/gut is deliberately committed - it's the test framework.
/addons/godot-git-plugin/
